//Split and merge
//void merge(image1, image2)
//  for(size_t i = image1 width; i < image2 width; i++)
//      add image2pixesl to image1
// 
//uint32[] splitandmerge(image, threshold)
//  create an empty array for the differents parts
//  divide the image using the threshol as dimensions by adding the different parts in the list
//  then traverse the list, and merge adjacent images that contain blanks
//  return the array
//
//
//uint32[] simpletraversal(image, leftcornerx, leftcornery, widthcell, heightcell)
//   create empty array
//   create empty array
//   for (int i = 0; i < widthcell; i++)
//      for(int j = 0; i < heightcell; j++)
//          change pixels from blank image to image[i][j]
//   add to array
//   reset image   
//   repeat by changing coordinates 
//    
